publishing {
	publications {
		style(MavenPublication) {
			groupId = "${group}"
			artifactId = 'clearth-style'
			version = "${version}"
			
			from components.java
		}
	}
}

artifactoryPublish {
	publications(publishing.publications.style)
}