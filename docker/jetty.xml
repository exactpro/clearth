<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- =============================================================== --><!-- Documentation of this file format can be found at:              --><!-- https://www.eclipse.org/jetty/documentation/current/       --><!--                                                                 --><!-- Additional configuration files are available in $JETTY_HOME/etc --><!-- and can be mixed in. See start.ini file for the default         --><!-- configuration files.                                            --><!--                                                                 --><!-- For a description of the configuration mechanism, see the       --><!-- output of:                                                      --><!--   java -jar start.jar -?                                        --><!-- =============================================================== --><!-- =============================================================== --><!-- Configure a Jetty Server instance with an ID "Server"           --><!-- Other configuration files may also configure the "Server"       --><!-- ID, in which case they are adding configuration to the same     --><!-- instance.  If other configuration have a different ID, they     --><!-- will create and configure another instance of Jetty.            --><!-- Consult the javadoc of o.e.j.server.Server for all              --><!-- configuration that may be set here.                             --><!-- =============================================================== --><!DOCTYPE Configure PUBLIC "-//Jetty//Configure//EN" "http://www.eclipse.org/jetty/configure_9_3.dtd">
<Configure class="org.eclipse.jetty.server.Server" id="Server">
    <Arg name="threadpool">
        <Ref refid="threadPool"/>
    </Arg>

    <Call name="addBean">
      <Arg>
            <Ref refid="byteBufferPool"/>
        </Arg>
    </Call>

    <!-- =========================================================== -->
    <!-- Add shared Scheduler instance                               -->
    <!-- =========================================================== -->
    <Call name="addBean">
      <Arg>
        <New class="org.eclipse.jetty.util.thread.ScheduledExecutorScheduler"/>
      </Arg>
    </Call>

    <!-- =========================================================== -->
    <!-- Http Configuration.                                         -->
    <!-- This is a common configuration instance used by all         -->
    <!-- connectors that can carry HTTP semantics (HTTP, HTTPS, etc.)-->
    <!-- It configures the non wire protocol aspects of the HTTP     -->
    <!-- semantic.                                                   -->
    <!--                                                             -->
    <!-- This configuration is only defined here and is used by      -->
    <!-- reference from other XML files such as jetty-http.xml,      -->
    <!-- jetty-https.xml and other configuration files which         -->
    <!-- instantiate the connectors.                                 -->
    <!--                                                             -->
    <!-- Consult the javadoc of o.e.j.server.HttpConfiguration       -->
    <!-- for all configuration that may be set here.                 -->
    <!-- =========================================================== -->
    <New class="org.eclipse.jetty.server.HttpConfiguration" id="httpConfig">
      <Set name="secureScheme">
            <Property default="https" name="jetty.httpConfig.secureScheme"/>
        </Set>
      <Set name="securePort">
            <Property default="8443" deprecated="jetty.secure.port" name="jetty.httpConfig.securePort"/>
        </Set>
      <Set name="outputBufferSize">
            <Property default="32768" deprecated="jetty.output.buffer.size" name="jetty.httpConfig.outputBufferSize"/>
        </Set>
      <Set name="outputAggregationSize">
            <Property default="8192" deprecated="jetty.output.aggregation.size" name="jetty.httpConfig.outputAggregationSize"/>
        </Set>
      <Set name="requestHeaderSize">
            <Property default="8192" deprecated="jetty.request.header.size" name="jetty.httpConfig.requestHeaderSize"/>
        </Set>
      <Set name="responseHeaderSize">
            <Property default="8192" deprecated="jetty.response.header.size" name="jetty.httpConfig.responseHeaderSize"/>
        </Set>
      <Set name="sendServerVersion">
            <Property default="true" deprecated="jetty.send.server.version" name="jetty.httpConfig.sendServerVersion"/>
        </Set>
      <Set name="sendDateHeader">
            <Property default="false" deprecated="jetty.send.date.header" name="jetty.httpConfig.sendDateHeader"/>
        </Set>
      <Set name="headerCacheSize">
            <Property default="4096" name="jetty.httpConfig.headerCacheSize"/>
        </Set>
      <Set name="delayDispatchUntilContent">
            <Property default="true" deprecated="jetty.delayDispatchUntilContent" name="jetty.httpConfig.delayDispatchUntilContent"/>
        </Set>
      <Set name="maxErrorDispatches">
            <Property default="10" name="jetty.httpConfig.maxErrorDispatches"/>
        </Set>
      <Set name="blockingTimeout">
            <Property default="-1" deprecated="jetty.httpConfig.blockingTimeout" name="jetty.httpConfig.blockingTimeout.DEPRECATED"/>
        </Set>
      <Set name="persistentConnectionsEnabled">
            <Property default="true" name="jetty.httpConfig.persistentConnectionsEnabled"/>
        </Set>
      <Set name="requestCookieCompliance">
            <Call class="org.eclipse.jetty.http.CookieCompliance" name="valueOf">
                <Arg>
                    <Property default="RFC6265" deprecated="jetty.httpConfig.cookieCompliance" name="jetty.httpConfig.requestCookieCompliance"/>
                </Arg>
            </Call>
        </Set>
      <Set name="responseCookieCompliance">
            <Call class="org.eclipse.jetty.http.CookieCompliance" name="valueOf">
                <Arg>
                    <Property default="RFC6265" name="jetty.httpConfig.responseCookieCompliance"/>
                </Arg>
            </Call>
        </Set>
      <Set name="multiPartFormDataCompliance">
            <Call class="org.eclipse.jetty.server.MultiPartFormDataCompliance" name="valueOf">
                <Arg>
                    <Property default="RFC7578" name="jetty.httpConfig.multiPartFormDataCompliance"/>
                </Arg>
            </Call>
        </Set>
    </New>

    <!-- =========================================================== -->
    <!-- Set the default handler structure for the Server            -->
    <!-- A handler collection is used to pass received requests to   -->
    <!-- both the ContextHandlerCollection, which selects the next   -->
    <!-- handler by context path and virtual host, and the           -->
    <!-- DefaultHandler, which handles any requests not handled by   -->
    <!-- the context handlers.                                       -->
    <!-- Other handlers may be added to the "Handlers" collection,   -->
    <!-- for example the jetty-requestlog.xml file adds the          -->
    <!-- RequestLogHandler after the default handler                 -->
    <!-- =========================================================== -->
    <Set name="handler">
      <New class="org.eclipse.jetty.server.handler.HandlerCollection" id="Handlers">
        <Set name="handlers">
         <Array type="org.eclipse.jetty.server.Handler">
                    <Item>
    <New class="org.eclipse.jetty.server.handler.ContextHandler">
        <Set name="contextPath">/clearth/reports</Set>
        <Set name="handler">
            <New class="org.eclipse.jetty.server.handler.ResourceHandler">
                <Set name="directoriesListed">false</Set>
                <Set name="resourceBase">/clearth/persistentFiles/automation/reports</Set>
            </New>
        </Set>
    </New>
</Item>
           <Item>
             <New class="org.eclipse.jetty.server.handler.ContextHandlerCollection" id="Contexts"/>
           </Item>
           <Item>
             <New class="org.eclipse.jetty.server.handler.DefaultHandler" id="DefaultHandler"/>
           </Item>
         </Array>
        </Set>
      </New>
    </Set>

    <!-- =========================================================== -->
    <!-- extra server options                                        -->
    <!-- =========================================================== -->
    <Set name="stopAtShutdown">
        <Property default="true" name="jetty.server.stopAtShutdown"/>
    </Set>
    <Set name="stopTimeout">
        <Property default="5000" name="jetty.server.stopTimeout"/>
    </Set>
    <Set name="dumpAfterStart">
        <Property default="false" deprecated="jetty.dump.start" name="jetty.server.dumpAfterStart"/>
    </Set>
    <Set name="dumpBeforeStop">
        <Property default="false" deprecated="jetty.dump.stop" name="jetty.server.dumpBeforeStop"/>
    </Set>

</Configure>
